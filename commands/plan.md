---
description: Break a feature request into an implementation plan using the feature-planner agent
---

Use the feature-planner agent to break down the following into a concrete, sequenced implementation plan. Flag any task that should be routed to a specialized reviewer (security, architecture, Go, Rust, e2e testing, Web3) once implemented.

Request: $ARGUMENTS
