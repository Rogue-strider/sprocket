---
description: Diagnose and fix the current build/test failure using the build-doctor agent
---

Use the build-doctor agent to diagnose and fix the current build or test failure. Run the project's build/test command first to capture the actual current error rather than assuming from any previously pasted output.

Additional context (paste the error, or leave blank to have build-doctor find it): $ARGUMENTS
